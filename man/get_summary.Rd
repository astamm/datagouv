% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-functions.R
\name{get_summary}
\alias{get_summary}
\title{Compute summary metrics for a dataset}
\usage{
get_summary(x, name = NULL)
}
\arguments{
\item{x}{A data frame or tibble (a single table, e.g. one element of the
list returned by \code{\link[=dg_pull_dataset]{dg_pull_dataset()}}).}

\item{name}{An optional label attached to the result (e.g. the dataset
title). When \code{NULL} (the default), the label is taken from the expression
passed to \code{x} when possible.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with a single row and the following columns:
\code{dataset}, \code{size_kb}, \code{n_vars}, \code{n_numeric}, \code{n_non_numeric}, \code{n_rows}
and \code{prop_missing}.
}
\description{
Computes key metrics describing a parsed dataset: its in-memory weight in
kilobytes, the number of variables, the number of numeric and non-numeric
variables, the number of rows and the proportion of missing values.
}
\examples{
get_summary(iris, name = "iris")
}
