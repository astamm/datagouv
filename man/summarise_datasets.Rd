% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-functions.R
\name{summarise_datasets}
\alias{summarise_datasets}
\title{Summarise several datasets}
\usage{
summarise_datasets(datasets = NULL, n = 100)
}
\arguments{
\item{datasets}{Either a named list of tibbles (each element is a single
table, named after it), a named list of such lists (as returned by
\code{\link[=dg_pull_dataset]{dg_pull_dataset()}}/\code{\link[=dg_download_many]{dg_download_many()}}, where a ZIP may contribute
several tables), a tibble from \code{\link[=dg_list_datasets]{dg_list_datasets()}} (identified by its
\code{id} column; each dataset is downloaded and summarised), a character
vector of dataset identifiers (or exact titles), or \code{NULL} (the default)
to use the first \code{n} datasets from \code{\link[=dg_list_datasets]{dg_list_datasets()}}.}

\item{n}{Number of datasets to summarise when \code{datasets} is \code{NULL}.
Defaults to \code{100}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per table and the columns
described in \code{\link[=get_summary]{get_summary()}}.
}
\description{
Applies \code{\link[=get_summary]{get_summary()}} to a collection of tables and combines the resulting
metrics into a single tibble. If \code{datasets} is \code{NULL}, the first \code{n} datasets
returned by \code{\link[=dg_list_datasets]{dg_list_datasets()}} are downloaded and summarised.
}
\examples{
# Summarise in-memory tables (no network needed).
summarise_datasets(datasets = list(iris = iris, mtcars = mtcars))

\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Download and summarise the first datasets of the catalog.
summarise_datasets()
\dontshow{\}) # examplesIf}
}
